!!!
%html
  %head
    %title Arcader
    = stylesheet_link_tag "application", :media => "all"
    = javascript_include_tag "application"
    = csrf_meta_tags
  %body
    %header.navbar.navbar-fixed-top
      %nav.navbar-inner
        .container
          %h2 Arcader
          %ul.nav
            = form_tag games_path, :class => "navbar-search pull-right", :method => :get do
              = text_field_tag :name, nil, :class => "search-query span3", :placeholder => "Search"
            %li.scores
              = link_to "Scores", users_path(@users)
            %li
              = link_to "Users", users_path(@users)
            %li
              = link_to "Games", games_path(@games)
      %div
        %h1.logo
          = link_to "Arcader Games", games_path
    = yield
  %footer
    %div.footer
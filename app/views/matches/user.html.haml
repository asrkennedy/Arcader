%h2
  - @users.each do |user|
    %div.each_user
      - if user.user_image != nil
        %h4.left
          = image_tag user.user_image.thumb.url
        %h4.right
          = link_to user.username, create_matches_path(@game.id, user.id)
          %br